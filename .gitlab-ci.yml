stages:
  - build
  - deploy
  - production

build-job:
  stage: build
  only:
    - develop
    - master
  tags:
    - shell
  script:
    - docker-compose build
    - docker-compose push

deploy-job:
  stage: deploy
  only:
    - develop
    - master
  tags:
    - shell
  script:
    - ln -s /var/people.db $CI_PROJECT_DIR/dao/
    - docker-compose up -d
